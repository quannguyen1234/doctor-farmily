import { FC } from 'react';
import TableHistory from './TableHistory';

const DoctorHistory: FC = () => {
  return (
    <div>
      <TableHistory />
    </div>
  );
};

export default DoctorHistory;
